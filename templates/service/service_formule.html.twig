{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Ajout d'un service à une formule</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

{% endblock %}